#
# ┌───────────────────────────────────────────────────────────────────────────┐
# │ FILE: home/.nanorc                                                        │
# │ ZWECK: Modernisierung von GNU Nano                                        │
# │ STANDARDS: Plattformübergreifend (Linux & Git Bash), Modern Bindings      │
# └───────────────────────────────────────────────────────────────────────────┘

# ──────────────────────────────────────────────────────────────
# 1. EDITOR-VERHALTEN & FORMATIERUNG
# ──────────────────────────────────────────────────────────────
set autoindent          # Einrückung der Vorzeile beibehalten
set tabsize 4           # Konsistent zu .editorconfig
set tabstospaces        # Leerzeichen statt Hard-Tabs (verhindert Diff-Chaos)
set softwrap            # Zeilenumbruch visuell (kein harter Break)
set atblanks            # Umbruch nur an Wortgrenzen (bessere Lesbarkeit)
set zap                 # Backspace löscht markierte Blöcke sofort
set noconvert           # Zeilenenden (LF/CRLF) beim Öffnen respektieren
set magic               # Erweiterte Dateityperkennung aktivieren
set locking             # Datei-Locking (.swp) zur Vermeidung von Edit-Konflikten

# ──────────────────────────────────────────────────────────────
# 2. INTERFACE (UI) & VISUALS
# ──────────────────────────────────────────────────────────────
set linenumbers         # Zeilennummern für effizientes Debugging
set constantshow        # Cursor-Position permanent in Statuszeile
set indicator           # Scroll-Indikator am rechten Rand
set minibar             # Kompakte Statuszeile (maximiert Editier-Fläche)
set stateflags          # Zeigt [Modifiziert], [Speichern] etc. oben rechts
set showcursor          # Cursor auch in Menüs/Hilfe sichtbar lassen

# ──────────────────────────────────────────────────────────────
# 3. HISTORIE & POSITIONIERUNG
# ──────────────────────────────────────────────────────────────
set historylog          # Such- und Befehlshistorie dauerhaft speichern
set positionlog         # Letzte Cursor-Position beim Wiederöffnen laden

# ──────────────────────────────────────────────────────────────
# 4. SYNTAX-HIGHLIGHTING (PLATFORM-AWARE)
# ──────────────────────────────────────────────────────────────
# Inkludiert verfügbare Syntax-Definitionen für verschiedene Umgebungen.

# Standardpfad (Linux/Debian/Ubuntu)
include "/usr/share/nano/*.nanorc"
include "/usr/share/nano-syntax-highlighting/*.nanorc"

# Git Bash / MSYS2 spezifisch
include "/etc/nanorc"

# Lokale Erweiterungen (falls vorhanden)
include "~/.nano/*.nanorc"

# ──────────────────────────────────────────────────────────────
# 5. MODERN BINDINGS (SHORTCUTS)
# ──────────────────────────────────────────────────────────────
# Ermöglicht intuitive Steuerung (Windows-Standard / Common Shortcuts).
# WICHTIG: Erfordert 'stty -ixon' in der .bashrc.

bind M-C copy main           # Alt+C: Kopieren (nach Markierung mit M-A)
bind M-V paste all           # Alt+V: Einfügen
bind ^F whereis all          # Ctrl+F: Suchen (statt Ctrl+W)
bind ^H replace main         # Ctrl+H: Ersetzen (statt Ctrl+\)
bind ^S savefile main        # Ctrl+S: Schnellspeichern (ohne Exit)
bind ^Q exit all             # Ctrl+Q: Beenden (statt Ctrl+X)
bind ^Z undo main            # Ctrl+Z: Rückgängig
bind ^Y redo main            # Ctrl+Y: Wiederholen
bind ^D zap main             # Ctrl+D: Zeichen/Block löschen (ohne Cut-Buffer)

# ──────────────────────────────────────────────────────────────
# 6. DESIGN (FARBSCHEMA v1.2.1)
# ──────────────────────────────────────────────────────────────
# Harmonisiert mit dem UI-Konzept aus libcolors.sh

set titlecolor black,cyan     # Kopfzeile (Dezent/Professionell)
set statuscolor black,cyan    # Statusleiste unten
set keycolor white,blue       # Shortcut-Tasten in der Hilfe
set numbercolor yellow,normal # Zeilennummern (Gelb für Fokus)
set errorcolor white,red      # Fehlermeldungen (Klassisches Warnrot)
set selectedcolor black,white # Markierter Text (Invertiert)
set stripecolor ,yellow       # (Optional) Markierung der 80-Zeichen Grenze
