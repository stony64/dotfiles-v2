#
# ┌───────────────────────────────────────────────────────────────────────────┐
# │ FILE: .gitignore                                                          │
# │ ZWECK: Schutz vor sensiblen Daten und System-Rückständen                  │
# │ STANDARDS: v1.2.1, Whitelist-Prinzip, Security-First                      │
# └───────────────────────────────────────────────────────────────────────────┘

# ──────────────────────────────────────────────────────────────
# 1. SENSIBLE DATEN & LOKALE KONFIGURATION
# ──────────────────────────────────────────────────────────────
# Alles, was '_local' im Namen trägt, bleibt auf deinem Rechner.
*_local
.bash*_local
.bash_history
.bash_sessions/
.ssh/
.gnupg/

# Credentials und Keys
.env
.netrc
*.key
*.pem
*.pub
*.p12
*.pfx

# ──────────────────────────────────────────────────────────────
# 2. EDITOREN & IDEs (VS CODE / NANO / VIM)
# ──────────────────────────────────────────────────────────────
# Wir ignorieren persönliche VS Code Einstellungen, behalten aber Projekt-Templates.
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/tasks.json
!*.code-workspace

# Temporäre Editor-Dateien (Swap, Backups)
*.swp
*.swo
*~
*.bak
*.old
*.orig
*.rej
.history/
.nfs*

# ──────────────────────────────────────────────────────────────
# 3. SYSTEM-BALLAST (OS-SPEZIFISCH)
# ──────────────────────────────────────────────────────────────
# macOS
.DS_Store
.AppleDouble
.LSOverride
# Windows
Thumbs.db
Desktop.ini
ehthumbs.db
$RECYCLE.BIN/
# Linux
.directory
.fuse_hidden*

# ──────────────────────────────────────────────────────────────
# 4. LOGS, EXPORTE & TEMPORÄRE DATEN
# ──────────────────────────────────────────────────────────────
*.log
*.tmp
.dotfiles_state

# Projekt-spezifische Exporte und Protokolle
_exports/
review_dump_*

# Test-Sandbox (Falls test_suite.sh lokal Dateien erzeugt)
test_sandbox/
tests/out/

# ──────────────────────────────────────────────────────────────
# 5. WHITELIST (EXPLIZITE REPOSITORY-DATEIEN)
# ──────────────────────────────────────────────────────────────
# Wir stellen sicher, dass die Kern-Architektur trotz globaler Regeln im Index bleibt.

# Skripte & Meta
!dotfilesctl.sh
!test_suite.sh
!.shellcheckrc
!.editorconfig
!.gitattributes
!.gitignore

# Verzeichnisse (Inhalt inkludieren)
!lib/
!lib/**
!home/
!home/**
!docs/
!docs/**
!config/
!config/*.example

# Sicherstellen, dass Dotfiles in home/ (trotz .bash* Mustern) erfasst werden
!home/.bashrc
!home/.bashenv
!home/.bashaliases
!home/.bashfunctions
!home/.bashexports
!home/.bashprompt
!home/.dircolors
!home/.nanorc
