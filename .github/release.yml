# ┌───────────────────────────────────────────────────────────────────────────┐
# │ FILE: .github/workflows/release.yml                                       │
# │ ZWECK: Automatisierte Validierung und Veröffentlichung (v1.2.1)           │
# │ FLOW: Trigger auf v*-Tags -> ShellCheck -> Sandbox Tests -> Release       │
# └───────────────────────────────────────────────────────────────────────────┘

name: Create Release

on:
  push:
    tags:
      - 'v*'

jobs:
  # ──────────────────────────────────────────────────────────────
  # 1. VALIDIERUNG (Quality Gate)
  # ──────────────────────────────────────────────────────────────
  test:
    name: Validate Scripts
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4

      - name: 🔍 ShellCheck (Static Analysis)
        uses: ludeeus/action-shellcheck@master
        with:
          # Scannt das Root-Verzeichnis und deine Bibliotheken (lib/)
          scandir: '.'
          # Nutzt deine .shellcheckrc für projektspezifische Ausnahmen
          ignore: 'node_modules'

      - name: 🧪 Run Test Suite (Functional Sandbox)
        run: |
          # Erzwinge Ausführungsrechte für die CI-Umgebung
          chmod +x dotfilesctl.sh test_suite.sh lib/*.sh
          # Führt die funktionale Validierung aus
          ./test_suite.sh

  # ──────────────────────────────────────────────────────────────
  # 2. RELEASE-JOB
  # ──────────────────────────────────────────────────────────────
  release:
    name: Create GitHub Release
    needs: test
    runs-on: ubuntu-latest
    permissions:
      contents: write # Erforderlich zum Erstellen von Releases

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Holt die gesamte Historie für korrekte Changelogs

      - name: 🚀 Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: Release ${{ github.ref_name }}
          body: |
            ## 🛠 Dotfiles Framework v1.2.1

            Dieses Release markiert den stabilen Meilenstein der modularen Architektur.
            Optimiert für **Linux (Ubuntu/Debian)** und **Windows (Git Bash/MSYS2)**.

            ### ✨ Highlights dieses Releases:
            - **Modulare Engine:** Klare Trennung von Logik (`lib/`) und Konfiguration (`home/`).
            - **Plattform-Agnostik:** Intelligente Pfad- und Symlink-Behandlung für NTFS/Unix.
            - **Quality Assurance:** Integrierte ShellCheck-Validierung und Sandbox-Tests.
            - **Dotfiles Control:** Erweiterter `doctor`-Befehl für Integritätsprüfung.

            ---
            *Automatisches Release generiert durch GitHub Actions.*
          generate_release_notes: true
          draft: false
          prerelease: false

      - name: ✅ Final Status
        if: success()
        run: echo "Release ${{ github.ref_name }} successfully published!"
