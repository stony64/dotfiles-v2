# ┌───────────────────────────────────────────────────────────────────────────┐
# │ FILE: .github/workflows/release.yml                                       │
# │ ZWECK: Automatisierte Validierung und Veröffentlichung (v1.2.2)            │
# │ FLOW: Trigger auf v*-Tags -> ShellCheck -> Sandbox Tests -> Release       │
# └───────────────────────────────────────────────────────────────────────────┘

name: Create Release

on:
  push:
    tags:
      - 'v*'

jobs:
  # ──────────────────────────────────────────────────────────────
  # 1. VALIDIERUNG (Quality Gate)
  # ──────────────────────────────────────────────────────────────
  test:
    name: Validate Scripts & Logic
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4

      - name: 🔍 ShellCheck (Static Analysis)
        uses: ludeeus/action-shellcheck@master
        with:
          # Scannt das Root-Verzeichnis und alle Bibliotheken (lib/)
          scandir: '.'
          # Nutzt die .shellcheckrc für projektspezifische Ausnahmen (z.B. SC1090)
          ignore: 'node_modules'

      - name: 🧪 Run Test Suite (Functional Sandbox)
        run: |
          # Erzwinge Ausführungsrechte für die CI-Umgebung
          chmod +x dotfilesctl.sh test_suite.sh lib/*.sh

          # Simulation der v1.2.2 Struktur:
          # Wir führen die Test-Suite direkt im Workspace aus.
          # Die Suite validiert die neue Pfad-Logik (REPO_ROOT Ermittlung).
          ./test_suite.sh

  # ──────────────────────────────────────────────────────────────
  # 2. RELEASE-JOB
  # ──────────────────────────────────────────────────────────────
  release:
    name: Create GitHub Release
    needs: test
    runs-on: ubuntu-latest
    permissions:
      contents: write # Erforderlich zum Erstellen von Releases und Tags

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Holt die gesamte Historie für korrekte Changelogs

      - name: 🚀 Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: Release ${{ github.ref_name }}
          body: |
            ## 🛠 Dotfiles Framework v1.2.2 (Stable)

            Dieses Release markiert den Übergang zur **zentralisierten Multi-User-Architektur**.
            Optimiert für den Einsatz unter **/opt/dotfiles** mit systemweitem Zugriff.

            ### ✨ Highlights in v1.2.2:
            - **Zentralisierung:** Framework-Kern für den Betrieb in `/opt/dotfiles` optimiert.
            - **Multi-User Support:** Neue Steuerung via `--user <name>` oder `--all-users`.
            - **Global CLI:** Vorbereitet für den systemweiten Aufruf via `dctl`.
            - **Namespace Protection:** Implementierung von `_VAL`-Suffixen zur Vermeidung von `readonly`-Kollisionen.
            - **Robust Path Finding:** Verbesserte Pfadauflösung, die Symlinks (readlink) zuverlässig folgt.

            ### 📦 Installation (Kurzform):
            1. `sudo git clone <url> /opt/dotfiles`
            2. `sudo ln -sf /opt/dotfiles/dotfilesctl.sh /usr/local/bin/dctl`
            3. `sudo dctl install --user $USER`

            ---
            *Automatisches Release generiert durch GitHub Actions (v1.2.2 Workflow).*
          generate_release_notes: true
          draft: false
          prerelease: false

      - name: ✅ Final Status
        if: success()
        run: echo "Release ${{ github.ref_name }} successfully published!"
